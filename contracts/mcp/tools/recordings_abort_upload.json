{
  "name": "recordings_abort_upload",
  "title": "Abort a recording upload",
  "description": "Cancels an unfinished direct recording upload grant after a failed local-file upload attempt, cleaning up stale multipart uploads.",
  "input_schema": {
    "type": "object",
    "properties": {
      "upload_token": {
        "type": "string"
      }
    },
    "required": [
      "upload_token"
    ],
    "additionalProperties": false
  },
  "errors": [
    "invalid_upload_token",
    "invalid_input"
  ],
  "examples": [],
  "output_schema": {
    "type": "object",
    "properties": {
      "ok": {
        "type": "boolean"
      }
    },
    "required": [],
    "additionalProperties": false
  }
}
