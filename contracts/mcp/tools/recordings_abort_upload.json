{
  "name": "recordings_abort_upload",
  "title": "Abort a recording upload",
  "description": "Cancel an unfinished direct recording upload grant. Use this after a failed local-file upload attempt so stale multipart uploads are cleaned up.",
  "input_schema": {
    "type": "object",
    "properties": {
      "upload_token": {
        "type": "string"
      }
    },
    "required": [
      "upload_token"
    ],
    "additionalProperties": false
  },
  "errors": [
    "invalid_upload_token",
    "invalid_input"
  ],
  "examples": [],
  "output_schema": {
    "type": "object",
    "properties": {
      "ok": {
        "type": "boolean"
      }
    },
    "required": [],
    "additionalProperties": false
  }
}
